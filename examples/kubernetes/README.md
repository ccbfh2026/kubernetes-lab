# How to Use the Script

1. Install PyYAML if you haven’t already:

  ```bash
  pip install PyYAML
  ```

2. Run the script:

  ```bash
  python split_yaml.py 01-pods.yaml 01-pods
  ```

3. Check the output in the `01-pods` directory.
